1 Режим (Генерация базы). (InProgress)
	Добавляем в alphas_simulate простенькие альфы со статусом InProgress
	Более-менее неплохие попадают в Prod, неочень - в Trash

2 Режим (Режим тухачевского) (InTuch)
	Берем альфу из prod, размешиваем ее вместе с 12 из Trash, добавляем в alphas_simulate со статусом InTuch.
	Симулируем ее, после смотрим в Prod(из Trash все удаляем) и выбираем только одну самую лучшую по Sharpe.
	C новой альфой повторяем процедуру.

3 Режим (Режим турнира) (InTour)
	Берем альфы из prod, сортируем их по Sharpe и начинаем турнирно строить мега альфу точно так же с итерациями

4 Режим (Режим смеси)
	Генерируем случайную величину и смешиваем все подряд уже хорошее с помощью нее. 	